def invertir_cadena(cadena):
    return cadena[::-1]

def contar_vocales(cadena):
   vocales = 0
   for letter in cadena:
      if letter in ['a', 'e', 'i', 'o', 'u']:
          vocales += 1
   return vocales
